Goal

Build a GraphQL API using Express + Apollo Server that exposes token analytics data, where the client chooses what data to fetch.

No frontend required. You’ll test everything via GraphQL Playground / Apollo Sandbox.
